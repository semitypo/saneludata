# System dependency (install before pip packages):
#   apt-get install -y espeak-ng libespeak-ng-dev
#
# Local setup (conda env with Python 3.11):
#   conda create -n tts-radio python=3.11 -y
#   conda activate tts-radio
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
#
# Cloud (RunPod): run cloud_setup.sh — handles everything automatically.

piper-tts
piper-phonemize
datasets>=2.14.0
soundfile>=0.12.1
librosa>=0.10.0
numpy>=1.24.0
tqdm>=4.65.0
huggingface_hub>=0.20.0
